# Quick Sort

# Complexity:
# Worst case: O(n^2) (rare but can happen)
# Avg. case: O(nlog(n))
# Best case: O(nlog(n))

# Aux. space: O(n) (naive implementation)
# 			: O(log(n)) (better - Sedgwick came up with it)